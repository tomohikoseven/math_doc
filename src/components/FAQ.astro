---
/**
 * よくある質問（FAQ）を表示するためのコンポーネント。
 * 内部でStatementコンポーネント（variant="faq"）を使用している。
 */
import Statement from './Statement.astro';

interface Props {
  title: string;
}
const { title } = Astro.props;
---
<Statement title={title} variant="faq">
  <slot />
</Statement>
