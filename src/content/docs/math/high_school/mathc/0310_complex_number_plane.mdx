---
title: 複素数平面の基礎．
description: 高校数学における複素数平面について、数の掛け算を「回転と拡大・縮小」として捉える基礎概念を解説します。
lastUpdated: 2026-06-06
schema:
  - "@type": "TechArticle"
    "headline": "複素数平面の基礎．"
    "description": "高校数学における複素数平面について、数の掛け算を「回転と拡大・縮小」として捉える基礎概念を解説します。"
  - "@type": FAQPage
    mainEntity:
      - "@type": Question
        name: なぜベクトルではダメなのですか？複素数平面を使うメリットは何ですか？
        acceptedAnswer:
          "@type": Answer
          text: 平面ベクトルも図形を扱う強力な道具であるが、ベクトルには「点（ベクトル）同士の掛け算」が標準的な形では定義されておらず、図形を回転させるには「行列」などの別の道具を持ち出す必要がある。一方で、複素数は「数そのもの」に回転の性質（極形式の掛け算）が内蔵されているため、単なる代数的な掛け算だけで、図形を自由自在に回転させることができるのが最大のメリットである。
---
import FlatList from '/src/components/FlatList.astro';
import ArticleTip from '/src/components/ArticleTip.astro';
import Statements from '/src/components/Statements.astro';
import Statement from '/src/components/Statement.astro';
import FAQ from '/src/components/FAQ.astro';

<ArticleTip title="複素数平面の基礎は、掛け算を「回転」と「拡大・縮小」に翻訳することである。">
　複素数平面の基礎は、<b>数の掛け算が図形的な「回転」と「拡大・縮小」として機能する</b>ことを理解し、図形を計算で操作できるようになることである。
</ArticleTip>

## なぜ「複素数」で「平面」を考えるのか？
　実数の世界では、数は1本の直線（数直線）の上に並んでいた。実数における掛け算、例えば「$-1$ を掛ける」という操作は、原点を中心にして数直線を<b>180度反転（回転）させる</b>ことを意味する。

　では、「2回掛けると $-1$ になる数」である虚数単位 $i$ とは何だろうか。図形的に考えれば、それは「2回掛けると180度回転する」、つまり<b>「1回掛けると90度回転する」</b>という動きを持つ数に他ならない。

　数直線上の数に $i$ を掛けて90度回転させると、数は直線を飛び出し、縦方向への広がりを持つ。こうして、数は1次元の直線から、2次元の「複素数平面」という新しい広がりを獲得したのである。

## 基礎となる2つの図形的翻訳
　複素数平面を使いこなすための基礎は、複素数の計算を「図形的な動き」に翻訳する以下の2つの視点を持つことである。

<Statements>
<Statement title="1. 足し算・引き算 ＝ ベクトル（平行移動）" variant="example">
　複素数の足し算 $z_1 + z_2$ や引き算 $z_1 - z_2$ は、図形ベクトルと全く同じ機能を持つ。つまり、ある点を別の場所へ<b>「平行移動」</b>させる操作である。
</Statement>

<Statement title="2. 掛け算・割り算 ＝ 極形式（回転と拡縮）" variant="example">
　複素数 $z$ を、原点からの距離 $r$ と、基準からの回転角（偏角） $theta$ を用いて次のように表すことを<b>「極形式」</b>と呼ぶ。

$$
z = r(cos theta + i sin theta)
$$

　極形式で表された複素数どうしを掛け算すると、距離（$r$）は掛け算され、角度（$theta$）は足し算される。
　つまり、複素数の掛け算とは<b>「ある点を $r$ 倍に拡大（縮小）し、$theta$ だけ回転させる」</b>というダイナミックな図形操作そのものなのである。
</Statement>

<Statement title="掛け算による図形操作の具体例" variant="example">
　例えば、ある複素数 $z$ に $1 + i$ を掛けるという計算を考えてみよう。
　$1 + i$ を極形式に直すと、原点からの距離が $sqrt(2)$、回転角が $45 degree$ となる。

$$
1 + i = sqrt(2) (cos 45 degree + i sin 45 degree)
$$

　したがって、$z times (1 + i)$ という単なる代数的な掛け算は、図形的には<b>「点 $z$ を原点を中心に $45 degree$ 回転させ、原点からの距離を $sqrt(2)$ 倍に拡大する」</b>という明確な操作に完全に翻訳される。
</Statement>
</Statements>

## 複素数平面で何がしたいのか？
　この「掛け算＝回転」という性質を利用することで、図形を扱う力が劇的に飛躍する。

　例えば、図形を何度も回転させる（何乗もする）計算は、角度の足し算に変換されるため、計算が劇的にシンプルになる（ド・モアブルの定理）。
　また、図形問題において「2つの直線が直交する」ことや「三角形の形を決定する」といった図形的な条件を、作図に頼らず<b>「掛け算による回転角」という代数的な計算だけで解き明かす</b>ことができるのが最大の目的である。

## よくある質問（FAQ）

<Statements>
<FAQ title="Q. なぜベクトルではダメなのですか？複素数平面を使うメリットは何ですか？">
　平面ベクトルも図形を扱う強力な道具であるが、ベクトルには「点（ベクトル）同士の掛け算」が標準的な形では定義されておらず、図形を回転させるには「行列」などの別の道具を持ち出す必要がある。一方で、複素数は「数そのもの」に回転の性質（極形式の掛け算）が内蔵されているため、単なる代数的な掛け算だけで、図形を自由自在に回転させることができるのが最大のメリットである。
</FAQ>
</Statements>

## まとめ
複素数平面の基礎については以下の通りである。
<FlatList>
- $i$ を掛けることは「90度回転」を意味し、これによって数は1次元の直線から2次元の平面へと広がった。
- 複素数の足し算・引き算は「平行移動（ベクトル）」として図形的に解釈できる。
- 複素数の掛け算・割り算は「極形式」を用いることで、「拡大・縮小」と「回転」という図形操作として完全に翻訳できる。
- 複素数平面を学ぶ目的は、図形の回転や角度に関する問題を、作図ではなく「掛け算」という代数計算の力で解決することにある。
</FlatList>
