---
title: P26 問108 マグロウヒル大学演習シリーズ 微積分(上)
description: P26 問108 （マグロウヒル大学演習シリーズ 微積分(上)）を解く．
lastUpdated: 2025-04-11
---

import { Card } from "@astrojs/starlight/components";

<Card title="問108">
　(a) $sqrt(5) + sqrt(3)$，(b) $sqrt(3) - sqrt(2)$，(c) $(sqrt(3))(sqrt(2))$，
(d) $frac(sqrt(2), sqrt(3))$は，それぞれデデキントの切断をどのように使えば定義できるか例示せよ．
</Card>

　$sqrt(5) "，" sqrt(3) "，" sqrt(2)$の有理数の切断を以下のとおりに表す．
$$
sqrt(5) = angle.l A, A' angle.r "，" A = {r in QQ thin|thin r < sqrt(5)} "，" A' = A "の補集合" \
sqrt(3) = angle.l B, B' angle.r "，" B = {r in QQ thin|thin r < sqrt(3)} "，" B' = B "の補集合" \
sqrt(2) = angle.l C, C' angle.r "，" C = {r in QQ thin|thin r < sqrt(2)} "，" C' = C "の補集合"
$$
### (a)
　$sqrt(5) + sqrt(3) = angle.l A + B, A' + B' angle.r$，$A + B = {a + b in QQ thin|thin a in A "，" b in B}$，
$A' + B'$は$A + B$の補集合．

### (b)
　$-sqrt(2) = angle.l - C', - C angle.r$，$-C' = {-s in QQ thin|thin s in C'}$，
$-C = {-s in QQ thin|thin s in C}$となる．
したがって，　$sqrt(3) - sqrt(2) = angle.l B - C', B' - C angle.r$，
$B - C' = {r - s in QQ thin|thin r in B "，" s in C'}$，$B' - C$は$B - C'$の補集合．

### (c)
　$(sqrt(3))(sqrt(2)) = angle.l B "・" C, B' "・" C' angle.r$，
$B "・" C = {r s in QQ thin|thin r in B "，" s in C}$，
$B' "・" C'$は$B "・" C$の補集合．

### (d)
　$frac(sqrt(2), sqrt(3)) = frac(sqrt(3) sqrt(2), 3)$なので，
$frac(sqrt(3) sqrt(2), 3) = angle.l D, D' angle.r$，
$D = {frac(r s, 3) in QQ thin|thin r in B "，" s in C}$，
$D'$は$D$の補集合．